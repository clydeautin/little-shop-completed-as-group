<%# locals: (url:, method:, button_text:) -%>

<%= form_with model: @merchant, url: url, method: method, local: true, data: { turbo: false } do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br>
  <%= f.submit button_text %>
<% end %>