<%# locals: (url:, method:, button_text:) -%>

<%= form_with url: url, method: method, local: true, data: { turbo: false } do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, value: (@merchant.blank? ? "" : "#{@merchant.name}") %>
  <br>
  <%= f.submit button_text %>
<% end %>